rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write to all collections for development/testing
    match /{document=**} {
      allow read, write: if true;
    }
  }
}